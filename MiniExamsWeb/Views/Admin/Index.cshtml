<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        /* General Body Styling */
        body {
            font-family: 'Comic Sans MS', cursive, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #eeeeee; /* Primary color: white */
        }

        /* Header Styling */
        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background-color: #005cbf; /* Secondary color: Pepsi blue */
            color: white;
        }

            .admin-header h1 {
                margin: 0;
                font-size: 2em;
            }

        .logout-button {
            background-color: #ff4500; /* Bright orange */
            border: none;
            color: white;
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1em;
            transition: background-color 0.3s ease;
        }

            .logout-button:hover {
                background-color: #ff6347; /* Lighter orange */
            }

        /* Admin Container Styling */
        .admin-container {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            padding: 40px;
        }

        /* Admin Card Styling */
        .admin-card {
            background: #ffffff; /* White background */
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            padding: 30px;
            width: 300px;
            height: 200px;
            text-align: center;
            cursor: pointer;
            border: 4px solid #005cbf; /* Pepsi blue border */
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

            .admin-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 12px rgba(0,0,0,0.3);
            }

            .admin-card h2 {
                color: #005cbf; /* Pepsi blue */
                margin-bottom: 10px;
                font-size: 1.8em;
            }

            .admin-card p {
                color: #4682b4; /* Steel blue */
                font-size: 1em;
            }

        /* User Card Specific Styling */
        .users-card {
            border-color: #ff69b4; /* Hot pink border */
        }

        /* Exam Card Specific Styling */
        .exams-card {
            border-color: #32cd32; /* Lime green border */
        }
    </style>
</head>
<body>
    <!-- Header with Logout Button -->
    <header class="admin-header">
        <h1>Admin Dashboard</h1>
        <button class="logout-button" onclick="location.href='/Home/Login'">Logout</button>
    </header>

    <!-- Admin Dashboard Cards -->
    <div class="admin-container">
        <div class="admin-card users-card" onclick="location.href='/Admin/EditUser'">
            <h2>Manage Users</h2>
            <p>View, add, edit, or delete users.</p>
        </div>
        <div class="admin-card exams-card" onclick="location.href='/Admin/EditExams'">
            <h2>Manage Exams</h2>
            <p>View, create, or update exams.</p>
        </div>
    </div>

    <script>
        // Example for future use if needed
        console.log("Admin Dashboard Loaded Successfully!");
    </script>
</body>
</html>